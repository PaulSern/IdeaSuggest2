<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="assignment.ideasuggest2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".dessert" />
        <activity android:name=".drink" />
        <activity android:name=".foodselection" />
        <activity android:name=".hawkercentre" />
        <activity android:name=".internationalfood" />
        <activity android:name=".nasikandar" />
        <activity android:name=".sushi" />
        <activity android:name=".fastfood"></activity>
    </application>

</manifest>